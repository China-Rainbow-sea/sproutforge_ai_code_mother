spring:
  application:
    name: sproutforge_ai_code_backend
server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.rainbowsea.sproutforgeaicodebackend.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
